{
	"$schema": "https://xript.dev/schema/mod/v0.7.json",
	"xript": "0.7",
	"name": "xoji-switch-default",
	"version": "0.0.1",
	"title": "Xoji Switch (default)",
	"description": "The built-in fill for the xoji Switch component: the mod-zero markup and behavior every app inherits and may override.",
	"capabilities": ["xoji.component.switch"],
	"entry": { "script": "mod.js", "format": "script" },
	"fills": {
		"component.switch": [
			{
				"id": "switch",
				"format": "text/html+jsml",
				"source": "switch.html",
				"handlers": [
					{ "selector": ".xoji-switch__track", "on": "click", "handler": "toggle" },
					{ "selector": ".xoji-switch__track", "on": "keydown", "handler": "keyToggle" }
				]
			}
		]
	}
}
