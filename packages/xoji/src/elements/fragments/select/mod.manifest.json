{
	"$schema": "https://xript.dev/schema/mod/v0.7.json",
	"xript": "0.7",
	"name": "xoji-select-default",
	"version": "0.0.1",
	"title": "Xoji Select (default)",
	"description": "The built-in fill for the xoji Select component: the mod-zero markup and behavior every app inherits and may override.",
	"capabilities": ["xoji.component.select"],
	"entry": { "script": "mod.js", "format": "script" },
	"fills": {
		"component.select": [
			{
				"id": "select",
				"format": "text/html+jsml",
				"source": "select.html"
			}
		]
	}
}
