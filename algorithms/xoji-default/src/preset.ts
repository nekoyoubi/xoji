/**
 * The neutral default xoji taste — pure data, no imports, so both the mod
 * (sibling `mod.ts`) and the engine's batteries bundle read one source.
 * Every value here is a `defineXojiAlgorithm` spec field; absent fields take
 * the shared xoji defaults.
 */
export const spec = { id: "xoji-default" };
